# AI-Vehicle-Research 저장소 규칙

차량용 AI 리서치 아카이브. 구조: `seminars/`(스터디 세미나) · `research/`(주제별 심층 리서치) · `news/`(데일리 뉴스 다이제스트).

## README 목차 동기화 (필수)

컨텐츠를 추가·이동·삭제·개명하는 모든 작업에서, 루트 `README.md`의 해당 목차 표를 같은 커밋에서 함께 갱신한다.

- **research/** 새 주제: 표에 행 추가 — 주제 링크(kebab-case 폴더명), 한 줄 요약(보고서 내용 기반, 핵심 키워드 포함), 작성 연월, 핵심 문서 링크. 최신이 위로 오도록 작성일 내림차순 정렬.
- **news/** 새 다이제스트: 날짜 행 추가, 최신이 위로.
- **seminars/** 새 회차: 회차 표에 행 추가, `seminars/README.md`도 함께 갱신.
- 삭제·개명 시 깨진 링크가 남지 않도록 표에서 제거·수정.

## 컨텐츠 규약

- 폴더명: kebab-case 영문 (예: `nvidia-alpamayo`).
- 주제 폴더 구성: 보고서 `.md`(원본) + `.html`(웹 버전, 있는 경우) + `images/`/`assets/`(그림) + `reference/`(출처·자막·원시 자료).
- 보고서 상단에 작성일과 출처 표기 원칙 유지 (모든 사실 주장에 출처 병기, 미확인은 "출처 미확인" 명시).
- `.DS_Store`, zip 스냅샷, 수동 백업 파일(`*_backup.*`)은 커밋하지 않는다 — 버전 관리는 git이 담당.
